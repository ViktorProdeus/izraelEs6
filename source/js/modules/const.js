const popupOrder = document.querySelector(`.popup--order`);
const popupSuccess = document.querySelector(`.popup--success`);
const submitBtns = document.querySelectorAll(`.submit-js`);

export {popupOrder, popupSuccess, submitBtns};
